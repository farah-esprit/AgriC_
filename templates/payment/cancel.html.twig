{% extends app.session.get('user_role') == 'FOURNISSEUR' ? 'fournisseur.html.twig' : (app.session.get('user_role') == 'AGRICULTEUR' ? 'agriculteur.html.twig' : 'base.html.twig') %}

{% block title %}Paiement Annulé{% endblock %}

{% block body %}
<section style="padding: 120px 0 60px;">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-6 text-center">
                <div class="card shadow border-0 p-5" style="border-radius: 15px;">
                    <div style="font-size: 80px;">❌</div>
                    <h2 class="text-danger fw-bold mt-3">Paiement Annulé</h2>
                    <p class="text-muted">Votre paiement a été annulé.</p>
                    <a href="{{ path('app_commande_index') }}" class="btn btn-outline-success mt-3">
                        <i class="bi bi-arrow-left"></i> Retour aux commandes
                    </a>
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}