# config/packages/notifier.yaml
framework:
  notifier:
    chatter_transports: ~
    texter_transports:
      twilio: '%env(TWILIO_DSN)%'
    channel_policy:
      urgent: ['sms/twilio']
      high: ['sms/twilio']
      medium: ['email']
      low: ['email']
    admin_recipients:
      - { email: admin@example.com }