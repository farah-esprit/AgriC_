{% extends 'agriculteur.html.twig' %}

{% block title %}Modifier l'Événement - AgriConnect{% endblock %}

{% block body %}
<div class="premium-header">
    <div class="container text-center">
        <h1 class="premium-title">Modifier l'Événement</h1>
        <p class="premium-subtitle">Mettez à jour les informations pour tenir la communauté informée.</p>
    </div>
</div>

<div class="container pb-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="form-premium-card shadow-lg border-0">
                <div class="mb-4 pb-3 border-bottom d-flex align-items-center gap-3">
                    <div class="bg-soft-agri p-3 rounded-circle" style="background: #fff9e6;">
                        <i class="bi bi-pencil-square fs-3 text-warning"></i>
                    </div>
                    <div>
                        <h4 class="fw-bold mb-0 text-dark">Détails de l'événement</h4>
                        <p class="text-muted small mb-0">Modifier les champs ci-dessous.</p>
                    </div>
                </div>

                {{ include('evenement/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
